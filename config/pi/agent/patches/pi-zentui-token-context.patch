diff -ru orig-pkg/extensions/zentui/editor-metadata-format.ts new-pkg/extensions/zentui/editor-metadata-format.ts
--- orig-pkg/extensions/zentui/editor-metadata-format.ts	2026-09-03 09:45:52.932971460 +0530
+++ new-pkg/extensions/zentui/editor-metadata-format.ts	2026-09-03 09:45:52.935720010 +0530
@@ -1,7 +1,12 @@
 import type { Theme } from "@earendil-works/pi-coding-agent";
 import type { ZentuiConfig } from "./config";
 import { type FormatToken, parseFooterFormat } from "./footer-format";
-import { buildSessionTokenLabel, formatCacheHitRate, formatContextPercentLabel } from "./format";
+import {
+	buildSessionTokenLabel,
+	formatCacheHitRate,
+	formatContextPercentLabel,
+	formatContextTokensLabel,
+} from "./format";
 import { EDITOR_ACCENT_FALLBACK, renderStyleForSourceOrFallback, safeThemeFg } from "./style";
 
 export type EditorMetadataValues = {
@@ -12,6 +17,7 @@
 	thinking: string;
 	sessionName: string;
 	contextPercent?: number;
+	contextTokens?: number;
 	contextWindow?: number;
 	inputTokens?: number;
 	outputTokens?: number;
@@ -176,8 +182,11 @@
 							? thinking
 							: name === "session_name"
 								? values.sessionName
-								: name === "context"
-									? formatContextPercentLabel(values.contextPercent, values.contextWindow)
+									: name === "context"
+										? values.contextTokens !== undefined &&
+										  Number.isFinite(values.contextTokens)
+											? formatContextTokensLabel(values.contextTokens, values.contextWindow)
+											: formatContextPercentLabel(values.contextPercent, values.contextWindow)
 									: name === "tokens"
 										? buildSessionTokenLabel({
 												input: values.inputTokens ?? 0,
diff -ru orig-pkg/extensions/zentui/footer.ts new-pkg/extensions/zentui/footer.ts
--- orig-pkg/extensions/zentui/footer.ts	2026-09-03 09:45:52.932971460 +0530
+++ new-pkg/extensions/zentui/footer.ts	2026-09-03 09:45:52.935720010 +0530
@@ -291,8 +291,10 @@
 					ctx,
 					hooks.getLiveContext?.(),
 				);
+				const contextTokens = hooks.getLiveContext?.()?.tokens ?? ctx.getContextUsage()?.tokens ?? undefined;
 				const contextLabel = buildContextDisplayLabel({
 					percent: contextPercent,
+					tokens: contextTokens,
 					contextWindow,
 					style: config.components.footer.styles.starship.contextStyle,
 					asciiGauge: iconMode === "ascii",
diff -ru orig-pkg/extensions/zentui/format.ts new-pkg/extensions/zentui/format.ts
--- orig-pkg/extensions/zentui/format.ts	2026-09-03 09:45:52.932971460 +0530
+++ new-pkg/extensions/zentui/format.ts	2026-09-03 09:45:52.935720010 +0530
@@ -358,19 +358,37 @@
 	return `${percentLabel}/${formatCount(contextWindow)}`;
 }
 
+export function formatContextTokensLabel(
+	tokens: number | null | undefined,
+	contextWindow: number | undefined,
+): string {
+	if (!contextWindow || contextWindow <= 0) return "--";
+	const used =
+		tokens === null || tokens === undefined || !Number.isFinite(tokens)
+			? "?"
+			: formatCount(Math.max(0, tokens));
+	return `${used}/${formatCount(contextWindow)}`;
+}
+
 export function buildContextDisplayLabel(options: {
 	percent: number | null | undefined;
 	contextWindow: number | undefined;
 	style?: ContextStyle;
 	asciiGauge?: boolean;
+	tokens?: number | null;
 }): string {
-	const { percent, contextWindow, style = "text", asciiGauge = false } = options;
+	const { percent, contextWindow, style = "text", asciiGauge = false, tokens } = options;
 	if (!contextWindow || contextWindow <= 0) return "--";
 
-	const text = formatContextPercentLabel(percent, contextWindow);
+	const hasTokens = tokens !== null && tokens !== undefined && Number.isFinite(tokens);
+	const text = hasTokens
+		? formatContextTokensLabel(tokens, contextWindow)
+		: formatContextPercentLabel(percent, contextWindow);
 	const numericPercent =
 		percent === null || percent === undefined || !Number.isFinite(percent)
-			? 0
+			? hasTokens
+				? Math.max(0, Math.min(100, (tokens / contextWindow) * 100))
+				: 0
 			: Math.max(0, Math.min(100, percent));
 	const gauge = buildContextGauge(numericPercent, 10, asciiGauge);
 
@@ -401,7 +419,10 @@
 
 export function buildContextLabel(ctx: ExtensionContext): string {
 	const { percent, contextWindow } = resolveContextUsage(ctx);
-	return formatContextPercentLabel(percent, contextWindow);
+	const tokens = ctx.getContextUsage()?.tokens;
+	return tokens === null || tokens === undefined || !Number.isFinite(tokens)
+		? formatContextPercentLabel(percent, contextWindow)
+		: formatContextTokensLabel(tokens, contextWindow);
 }
 
 export function formatRuntimeSegment(
diff -ru orig-pkg/extensions/zentui/index.ts new-pkg/extensions/zentui/index.ts
--- orig-pkg/extensions/zentui/index.ts	2026-09-03 09:45:52.932971460 +0530
+++ new-pkg/extensions/zentui/index.ts	2026-09-03 09:45:52.935720010 +0530
@@ -306,6 +306,14 @@
 		getContextSnapshot(ctx).contextWindow;
 	const getContextPercent = (ctx: ExtensionContext): number | undefined =>
 		getContextSnapshot(ctx).percent;
+	const getContextTokens = (ctx: ExtensionContext): number | undefined => {
+		const usage = ctx.getContextUsage();
+		const contextWindow = getContextWindow(ctx);
+		const live = liveContext.get();
+		return live && contextWindow && contextWindow > 0
+			? live.tokens
+			: (usage?.tokens ?? undefined);
+	};
 	const getEditorMeta = (ctx: ExtensionContext) => {
 		const context = getContextSnapshot(ctx);
 		return {
@@ -315,6 +323,7 @@
 			providerLabel: state.providerLabel,
 			sessionName: ctx.sessionManager.getSessionName() ?? "",
 			contextPercent: context.percent,
+			contextTokens: getContextTokens(ctx),
 			contextWindow: context.contextWindow,
 			inputTokens: state.usageTotals.input,
 			outputTokens: state.usageTotals.output,
@@ -727,6 +736,7 @@
 						modelLabel: modelLabelFor(state, currentConfig.components.editor.modelLabel),
 						thinkingLevel: getThinkingLevel(),
 						contextPercent: getContextPercent(ctx),
+						contextTokens: getContextTokens(ctx),
 						contextWindow: getContextWindow(ctx),
 						sessionName: ctx.sessionManager.getSessionName() ?? "",
 						agentDurationMs: getAgentDurationMs(),
@@ -766,6 +776,7 @@
 						modelLabel: modelLabelFor(state, currentConfig.components.editor.modelLabel),
 						thinkingLevel: getThinkingLevel(),
 						contextPercent: getContextPercent(ctx),
+						contextTokens: getContextTokens(ctx),
 						contextWindow: getContextWindow(ctx),
 						sessionName: ctx.sessionManager.getSessionName() ?? "",
 						agentDurationMs: getAgentDurationMs(),
diff -ru orig-pkg/extensions/zentui/minimalist-editor.ts new-pkg/extensions/zentui/minimalist-editor.ts
--- orig-pkg/extensions/zentui/minimalist-editor.ts	2026-09-03 09:45:52.932971460 +0530
+++ new-pkg/extensions/zentui/minimalist-editor.ts	2026-09-03 09:45:52.935720010 +0530
@@ -47,6 +47,7 @@
 	modelLabel?: string;
 	thinkingLevel?: string;
 	contextPercent?: number;
+	contextTokens?: number;
 	contextWindow?: number;
 	sessionName?: string;
 	agentDurationMs?: number;
@@ -204,8 +205,33 @@
 	if (thinking && thinking.toLowerCase() !== "off") {
 		parts.push(renderThinking(thinking));
 	}
-	if (metadata.contextPercent !== undefined && Number.isFinite(metadata.contextPercent)) {
-		const percent = Math.round(Math.max(0, Math.min(999, metadata.contextPercent)));
+	if (
+		metadata.contextPercent !== undefined ||
+		metadata.contextTokens !== undefined
+	) {
+		const contextPercent =
+			metadata.contextPercent !== undefined && Number.isFinite(metadata.contextPercent)
+				? metadata.contextPercent
+				: undefined;
+		const contextTokens =
+			metadata.contextTokens !== undefined && Number.isFinite(metadata.contextTokens)
+				? metadata.contextTokens
+				: undefined;
+		if (contextPercent === undefined && contextTokens === undefined) return joinParts(parts);
+		const contextWindow =
+			metadata.contextWindow !== undefined &&
+			Number.isFinite(metadata.contextWindow) &&
+			metadata.contextWindow > 0
+				? metadata.contextWindow
+				: undefined;
+		const percent =
+			contextPercent !== undefined
+				? Math.round(Math.max(0, Math.min(999, contextPercent)))
+				: contextWindow !== undefined && contextTokens !== undefined
+					? Math.round(
+							Math.max(0, Math.min(999, (contextTokens / contextWindow) * 100)),
+						)
+						: 0;
 		const tier = contextColorTier(
 			percent,
 			config.components.editor.styles.minimalist.contextThresholds,
@@ -218,12 +244,12 @@
 					: config.colors.contextNormal;
 		const total =
 			config.components.editor.styles.minimalist.contextFormat === "percent-total" &&
-			metadata.contextWindow !== undefined &&
-			Number.isFinite(metadata.contextWindow) &&
-			metadata.contextWindow > 0
-				? `/${formatCount(metadata.contextWindow)}`
+			contextWindow !== undefined
+				? `/${formatCount(contextWindow)}`
 				: "";
-		const text = `${percent}%${total}`;
+		const used =
+			contextTokens !== undefined ? formatCount(contextTokens) : `${percent}%`;
+		const text = `${used}${total}`;
 		let context = renderStyleForSource(uiTheme, source, style, text);
 		if (config.components.editor.styles.minimalist.contextGauge) {
 			for (const gaugeWidth of [5, 3]) {
diff -ru orig-pkg/extensions/zentui/settings-previews.ts new-pkg/extensions/zentui/settings-previews.ts
--- orig-pkg/extensions/zentui/settings-previews.ts	2026-09-03 09:45:52.932971460 +0530
+++ new-pkg/extensions/zentui/settings-previews.ts	2026-09-03 09:45:52.935720010 +0530
@@ -128,6 +128,7 @@
 				modelLabel,
 				thinkingLevel: "high",
 				contextPercent: 75,
+				contextTokens: 279_000,
 				contextWindow: 372_000,
 				sessionName: "Preview",
 				agentDurationMs: 12_000,
