{
  config,
  inputs,
  pkgs,
  ...
}:
{
  # services
  services = {
    #    emacs = {
    #      enable = true;
    #      client.enable =true;
    #      socketActivation.enable = true;
    #    };
    # # dunst = {
    #  enable = true;
    #  iconTheme = pkgs.papirus-icon-theme; 
    # };
  };
}
